<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-4">
  <div class="container">
    <a class="navbar-brand" href="/">Whats new</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link #{current_page?(root_path) ? 'active' : ''}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Users", users_path, class: "nav-link #{controller_name == 'users' ? 'active' : ''}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Messages", messages_path, class: "nav-link #{controller_name == 'messages' ? 'active' : ''}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Chats", chats_path, class: "nav-link #{controller_name == 'chats' ? 'active' : ''}" %>
        </li>
      </ul>

      <ul class="navbar-nav">
        <% if user_signed_in? %>
        <% if current_user.admin? %>
          <span class="badge bg-warning text-dark">Admin</span>
        <% end %>
          <li class="nav-item">
            <span class="navbar-text">Signed in as <%= current_user.email %></span>
          </li>
          <li class="nav-item">
            <%= link_to "Edit profile", edit_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, form_class: "d-inline", class: "nav-link btn btn-link", data: { turbo: false } %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
